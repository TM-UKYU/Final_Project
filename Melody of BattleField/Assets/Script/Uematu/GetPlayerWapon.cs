using System.Collections;
using System.Collections.Generic;
using UnityEngine;

class GetPlayerWapon : MonoBehaviour
{
    public enum Weapon
    {
        Nome,
        Guitar,
        Keyboard
    }

    public static Weapon weapon = Weapon.Nome;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
